package main;

pub fn main() -> void {
    let nums: [4]i32 = [10, 20, 30, 40];
    for x in nums {
        log(x);
    }

    let names: [3]string = ["Alice", "Bob", "Charlie"];
    for name in names {
        log(name);
    }

    return;
}
