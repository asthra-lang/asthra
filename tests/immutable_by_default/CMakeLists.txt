# Immutable By Default Tests Custom Configuration
# REMOVED: All immutable_by_default tests depend on deleted code_generator_types.h
# These tests need to be rewritten to work with the new LLVM backend interface

# The entire test suite is disabled until it can be updated to work with the LLVM backend